type MiningSubsystem struct {
    faults FaultSet

    CreateMiner(owner Address, worker Address) &StorageMinerActor
    AddSector(pledge TokenAmount, sectorID SectorID, comm SealCommitment, proof SealProof) SectorID
    ProveStorage(proofs PoStProof, doneSet Bitfield)
    CheckForFaults()
    ReportFaults(/**/)
    UpdateSectors()
    Depledge(amount TokenAmount)
}
