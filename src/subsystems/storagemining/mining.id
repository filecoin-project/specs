type PubKey struct {}
type StorageDeal struct {}
type TicketRandomness struct {}
type Ticket struct {}

type MiningSubsystem struct {
    // actors [&StorageMinerActor]
    // CreateMiner(owner Address, worker Address) &StorageMinerActor

    StartMining() struct {}
    StopMining() struct {}

    CreateMiner(ownerPubKey PubKey, workerPubKey PubKey, pledgeAmt TokenAmount) struct {}
    MadeDeal(deal StorageDeal)
    ScratchTickets(randomness TicketRandomness) [Ticket]
}
