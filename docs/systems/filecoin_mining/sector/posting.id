type PoStRandomness Bytes

// Can move to a new posting.id if we think worth it.
type PoStCfg struct {
    SectorSize      UInt
    SubsectorCount  UInt
    Partitions      UInt
}

type PoStWitness struct {
    Config PoStProofConfig
}
type PoStProof struct {//<curve, system> {
    Config      PoStProofConfig
    ProofBytes  Bytes
}

type PoStProofConfig struct {// TODO
}
